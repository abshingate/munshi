# Terraform state contains secrets and account details — never commit it
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*

# Your personal values (IP address) and the Windows password key
terraform.tfvars
*.pem
local/
vm/app/node_modules/
vm/app/package-lock.json

# Python
__pycache__/
*.py[cod]
